#include <stdio.h>

/**
 * main - prints exactly "programming is like building a multilingual puzzle"
 * Return: 0 if everything worked properly, non-zero otherwise
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
